tasks:
  - init: |
      export ROOTDIR=$(pwd)
      cd apps/backend && npm install && npm run start
  - command:      
      cd apps/storefront && yarn install && yarn dev
  - command:            
      cd apps/admin && yarn install && yarn start